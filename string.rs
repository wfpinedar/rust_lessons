// Un char es un solo carácter (Por ejemplo: 1, 6, %, b, p, ., T, etc.)

// El tipo String es un tipo especial que consiste en múltiples char.

// Para inicializar un valor de cadena en una variable, enciérrelo entre comillas dobles:

// let s1 = "This is a string";
// En el ejemplo anterior, se inicializa una variable de cadena llamada s1.

// A diferencia de los números, al trabajar con cadenas debemos tener más cuidado con los tipos.

// Si quieres declarar explícitamente un tipo String, necesitas convertir la cadena de esta manera:

// let s1: String = "This is a string".to_string();

fn main() {
    // Type your code below
    let coddy = "I am learning to code with Coddy!";
    
    // Don't change the line below
    println!("coddy = \"{}\"", coddy);
}